<%= form_with(model: [:backend, option_type, option_value], local: true, class: "option-value-form") do |form| %>
  <%= error_messages_for(option_value) %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class: "form-control", autocomplete: "off" %>
  </div>

  <div class="form-group">
    <%= form.label :presentation %>
    <%= form.text_field :presentation, class: "form-control", autocomplete: "off" %>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn-success" %>

    <% if option_value.persisted? %>
      <%= link_to(backend_option_type_option_value_path(option_type, option_value), method: :delete, class: "btn btn-outline-danger ml-3") do %>
        <%= material_icon "delete" %> Destroy
      <% end %>
    <% end %>

    <%= link_to(backend_option_type_option_values_path, class: "btn btn-outline-secondary ml-3") do %>
      <%= material_icon "clear" %> Cancel
    <% end %>
  </div>
<% end %>
